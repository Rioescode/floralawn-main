# Address Search Implementation Tasks

- [x] Update `formData` state in `app/contact/page.js` to include `state` and `zipCode`
- [x] Enhance `initAutocomplete` with Rhode Island bounds bias and full field capture
- [x] Update `place_changed` listener to map all location fields and set formatted address
- [x] Refactor the Address Input UI into a "Search" style bar
- [x] Add "Verified Property" visual feedback
- [x] Update `handleSubmit` and API payloads to use dynamic state/zip
- [x] Verify functionality
